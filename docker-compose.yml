services:
  conversor-dnd:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: conversor-dnd-web
    restart: unless-stopped
    ports:
      - "${DND_PORT:-5000}:5000"
    environment:
      DND_EDITOR_DIR: /app/editor
      DND_DATA_DIR: /app/data
      DND_OUTPUT_DIR: /app/output
      DND_TEMPLATES_DIR: /app/templates
      DND_FONTS_DIR: /app/fonts
      DND_STORAGE_LIMIT_BYTES: ${DND_STORAGE_LIMIT_BYTES:-1073741824}
      DND_EXPORT_TMP_BUDGET_BYTES: ${DND_EXPORT_TMP_BUDGET_BYTES:-67108864}
    volumes:
      - ${DND_HOST_STORAGE_ROOT:-/mnt/lv_hdd/dnd-conversor/personajes}/data:/app/data
      - ${DND_HOST_STORAGE_ROOT:-/mnt/lv_hdd/dnd-conversor/personajes}/output:/app/output
    healthcheck:
      test:
        - CMD-SHELL
        - python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5000/api/status', timeout=5)" >/dev/null 2>&1 || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
